{% extends 'base.html.twig' %}

{% block body %}
    <div id="wrapper">
        <div id="container">

            <div>
                <h1 class="text-center" >Welcome on the 4Troll website!</h1>
                {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                <p class="text-center"  >You can by now read the following posts or <a href= {{path('login')}}>login</a> and post your own!  </p>
                {% endif %}
            </div>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <div class="col-md-4 col-md-offset-4">
            	<a class="btn btn-primary col-md-12" href="{{path("newGag")}}" ><span class="glyphicon glyphicon-plus" ></span> ADD A POST</a>
        	</div>
            {% endif%}

        </div>
    </div>
{% endblock %}

{% block stylesheets %}
{% endblock %}
